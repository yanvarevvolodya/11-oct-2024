<Window x:Class="task_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:task_3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TabControl x:Name="tabcontrol1">
            <TabItem Name="tabitem1" Header="Вкладка 1">
                <Grid>
                    <Label Name ="label1" Content="Введите ваше имя:" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="100"/>
                    <Button Name="addButton" Content="Ввод" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="100" Margin="20" Click="addButton_Click"/>
                    <TextBox Name="textBox1" Height="25" Width="250" ToolTip="Введите ваше имя"/>
                </Grid>
            </TabItem>

            <TabItem Name="tabitem2" Header="Вкладка 2">
                <Grid>
                    <Slider x:Name="textSizeSlider" Minimum="8" Maximum="40" Value="{Binding TextSize, Mode=TwoWay}" 
                TickFrequency="1" IsSnapToTickEnabled="True" 
                VerticalAlignment="Bottom" Margin="20,0,0,23"/>
                    <TextBox FontSize="{Binding Text}" Margin="50,20,50,80" VerticalScrollBarVisibility="Auto"/>

                </Grid>
            </TabItem>

            <TabItem Name="tabitem3" Header="Вкладка 3">
                <Grid>

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
